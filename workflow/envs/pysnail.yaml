name: pysnail
channels:
  - conda-forge
  - bioconda
  - defaults
dependencies:
  - python=3.7.*
  - numpy
  - pandas
  - scipy
  - pyyaml  
  - bokeh
  - pip
  - pip:
      - git+https://github.com/kuijjerlab/PySNAIL.git